export class Item {

  title: string;
  units: number;
  category: string;

  constructor(title: string, units: number, category: string) {
    this.title = title;
    this.units = units;
    this.category = category;
  }
}
